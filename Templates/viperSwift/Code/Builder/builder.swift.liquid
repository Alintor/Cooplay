//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//

import Foundation
import UIKit
import SwiftUI
import Swinject

enum {{ module_info.name }}Builder {
    
    static func build() -> UIViewController {
        let r = ApplicationAssembly.assembler.resolver

        let viewModel = {{ module_info.name }}ViewModel()
        let interactor = {{ module_info.name }}Interactor()
        let router = {{ module_info.name }}Router()
        let presenter = {{ module_info.name }}Presenter(
            view: viewModel,
            interactor: interactor,
            router: router
        )
        let viewController = {{ module_info.name }}ViewController(
            contentView: {{ module_info.name }}View(viewModel: viewModel, output: presenter),
            output: presenter
        )
        router.rootController = viewController
        
        return viewController
    }
}